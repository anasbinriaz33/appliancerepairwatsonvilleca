import {site} from "@/lib/site";
export default function ServiceAreaStrip({ content }: { content: { eyebrow: string; heading: string } }){return <section className="border-b border-line/70 bg-brass text-ink"><div className="mx-auto flex max-w-content flex-col gap-4 px-5 py-6 sm:flex-row sm:items-center sm:justify-between lg:px-6"><div><p className="font-body text-[10px] font-black uppercase tracking-[.2em] opacity-70">{content.eyebrow}</p><p className="font-display text-xl font-bold">{content.heading}</p></div><div className="flex flex-wrap gap-x-5 gap-y-1 font-body text-xs font-semibold opacity-80">{site.serviceAreas.map(a=><span key={a}>{a}</span>)}</div></div></section>}
