import Image from "next/image"; import Link from "next/link"; import {site} from "@/lib/site"; import {services} from "@/lib/services";
export default function Footer(){return <footer className="border-t border-line/70 bg-[#08090a]"><div className="mx-auto grid max-w-content gap-10 px-5 py-14 sm:grid-cols-2 lg:grid-cols-4 lg:px-6"><div className="sm:col-span-2 lg:col-span-1"><div className="flex items-center gap-3"><Image src="/images/logo/Logo.png" alt={site.brandName} width={220} height={56} className="h-16 w-auto" /></div><p className="mt-5 max-w-xs font-body text-sm leading-6 text-muted">Local appliance repair with clear communication, honest pricing and dependable service.</p><a href={`tel:${site.phoneHref}`} className="mt-5 block font-body text-sm font-bold text-paper hover:text-brass">{site.phoneDisplay}</a></div><div><p className="font-body text-xs font-bold uppercase tracking-[.18em] text-muted">Services</p><ul className="mt-4 space-y-2">{services.map(s=><li key={s.slug}><Link href={`/services/${s.slug}`} className="font-body text-sm text-paper/70 hover:text-brass">{s.name}</Link></li>)}</ul></div><div><p className="font-body text-xs font-bold uppercase tracking-[.18em] text-muted">Service area</p><div className="mt-4 flex flex-wrap gap-x-3 gap-y-2">{site.serviceAreas.map(a=><span key={a} className="font-body text-sm text-paper/70">{a}</span>)}</div></div><div><p className="font-body text-xs font-bold uppercase tracking-[.18em] text-muted">Hours</p><ul className="mt-4 space-y-2">{site.hours.map(h=><li key={h.day} className="font-body text-sm text-paper/70"><span className="text-muted">{h.day}</span><br/>{h.time}</li>)}</ul></div></div><div className="border-t border-line/70"><div className="mx-auto flex max-w-content flex-col gap-2 px-5 py-5 font-body text-xs text-muted sm:flex-row sm:justify-between lg:px-6"><span>© {new Date().getFullYear()} {site.brandName}. All rights reserved.</span><span>Licensed & insured local appliance repair.</span></div></div></footer>}
